export enum Role {
  Admin = 'admin',
  Doctor = 'doctor',
  Assistant = 'assistant',
  Lab = 'lab',
  Patient = 'patient',
}

export const ALL_ROLES = [Role.Admin, Role.Doctor, Role.Assistant, Role.Lab, Role.Patient];
